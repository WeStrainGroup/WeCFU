name: wecfu
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - numpy
  - scipy
  - scikit-image
  - opencv
  - pillow
  - pandas
  - fastapi
  - uvicorn
  - python-multipart
  - pytest
# SAM (segment-anything + torch) is OPTIONAL and installed lazily on first use.
# Run: pip install segment-anything torch    # ~2 GB, only needed for dense plates
